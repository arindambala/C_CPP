// Rewrite the code using a switch statement

#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Cards program section

    char card_name[3];
    puts("\nEnter the card name: ");
    scanf("%2s", card_name);

    int val = 0;

    printf("The card value is: %i\n", val);

    return 0;
}